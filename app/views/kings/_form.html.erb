<%= form_for(@king) do |f| %>
  <% if @king.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@king.errors.count, "error") %> prohibited this king from being saved:</h2>

      <ul>
      <% @king.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.text_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
