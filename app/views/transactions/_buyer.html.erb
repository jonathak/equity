<% @entities.sort {|a,b| a.name.downcase <=> b.name.downcase}.each do |e| %>
  <% if e.id != @seller_id %>
    <option value=
    <%= "'#{e.id}'" %>  > 
    <%= "#{e.name}" %> 
    </option>
  <% end %>
<% end %>