<% company = session[:company_id].to_i.c %>

<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @entity.name %>
</p>

<p>
  <%= link_to("Invite #{@entity.name} to 77shares.", "/requests/new")  unless (@entity.investment || (@entity.name == company.name)) %>
</p>


<%= link_to 'Edit', edit_entity_path(@entity) %> |
<%= link_to 'Back', company %>
